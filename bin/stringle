#!/usr/bin/env node

'use strict';

var getrc = require('./getrc'),
    get = require('./get'),
    Promise = require('bluebird');


Promise.promisifyAll(require('fs'));

getrc(require('./config'))
  .then(get);
